package com.example.riderapp.activity;import com.example.riderapp.R;import com.example.riderapp.data.AppConfig;import com.example.riderapp.webservice.WsClient;import android.app.Activity;import android.os.AsyncTask;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;public class RegistActivity extends Activity {	String email;	String password;	String name;	EditText et_email;	EditText et_pwd;	EditText et_name;	@Override	protected void onCreate(Bundle savedInstanceState) {		// TODO Auto-generated method stub		super.onCreate(savedInstanceState);		setContentView(R.layout.registe_layout);		et_email = (EditText) findViewById(R.id.reg_email);		et_pwd = (EditText) findViewById(R.id.reg_pwd);		et_name = (EditText) findViewById(R.id.reg_name);		Button regBtn = (Button) findViewById(R.id.reg_okbtn);		regBtn.setOnClickListener(new View.OnClickListener() {			@Override			public void onClick(View v) {				email = et_email.getText().toString();				password = et_pwd.getText().toString();				name = et_name.getText().toString();				new RegistTask().execute(email, password, name);				finish();			}		});	}	class RegistTask extends AsyncTask<String, String, String> {		@Override		protected String doInBackground(String... params) {			WsClient ws = new WsClient(AppConfig.WSDL, AppConfig.WSDL_NAMESPACE);			ws.getSoapObject("regist", "email", params[0], "password",					params[1], "name", params[2]);			return null;		}		@Override		protected void onPostExecute(String result) {			// TODO Auto-generated method stub			super.onPostExecute(result);		}	}}